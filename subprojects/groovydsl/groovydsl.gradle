apply plugin: 'groovy'

description = 'pipeline DSL in groovy  --the DSL used by users to configure their pipelines'

dependencies {
    compile libraries.groovy
    compile project(':api')

    testCompile libraries.spock
}
